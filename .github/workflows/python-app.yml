name: Backend

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  lint-and-test-backend:
    # CI Machine OS
    runs-on: ubuntu-latest

    # MySQL service
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: testpassword
          DATABASE_HOST: 127.0.0.1
          DATABASE_USERNAME: testuser
          DATABASE_PASSWORD: testpassword
          DATABASE_NAME: testdb
        ports:
          - "3306:3306"
        options: "--health-cmd=\"mysqladmin ping -uroot -ptestpassword || exit 1\" --health-interval=10s --health-timeout=5s --health-retries=5"
        
    steps:
      # Checkout commit
      - uses: actions/checkout@v4

      # Install Python
      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      # Install Dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/dependencies.txt

      # Run linter
      - name: Lint with ruff
        run: ruff check

      # Run tests
      - name: Test with pytest
        env:
          DATABASE_URL: mysql+pymysql://testuser:testpassword@127.0.0.1:3306/testdb
        run: pytest

      # Start server
      - name: Start server
        working-directory: backend
        run: |
          uvicorn src.server:app --host 0.0.0.0 --port 8000 &
          sleep 10 

      - name: Check if server is running
        run: |
          sudo lsof -i :8000 || echo "Port 8000 not in use"

      # Perform Health Check
      - name: Perform Health Check
        env:
          DATABASE_HOST: 127.0.0.1
          DATABASE_USERNAME: testuser
          DATABASE_PASSWORD: testpassword
          DATABASE_NAME: testdb
        run: |
          sleep 5
          curl -v --fail http://localhost:8000/health
